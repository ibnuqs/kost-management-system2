# Domain-Ready Configuration Template
# Replace {DOMAIN} with your actual domain when ready

# Basic App Configuration
APP_NAME="Kost Management System"
APP_ENV=production
APP_KEY=base64:ZmmmfKvQ9/AheKNbjx8ZBYe2wPT3YX47tG/RP+Aq13s=
APP_DEBUG=false
APP_URL=https://{DOMAIN}
APP_TIMEZONE=Asia/Jakarta

APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

# Company Information (for receipts)
APP_ADDRESS="Jl. Contoh No. 123, Kota Contoh, 12345"
APP_PHONE="0812-3456-7890"
APP_EMAIL="info@{DOMAIN}"

APP_MAINTENANCE_DRIVER=file
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# Database Configuration - Production MySQL
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=kost_db
DB_USERNAME=kost_user
DB_PASSWORD=KostSecure123!

# Session & Cache - File based (NO Redis)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN={DOMAIN}
CACHE_STORE=file

# Broadcasting - Log only (NO Pusher for now)
BROADCAST_CONNECTION=log

# Queue - Database (NO Redis)
QUEUE_CONNECTION=database

# Filesystem
FILESYSTEM_DISK=local

# Memory Cache
MEMCACHED_HOST=127.0.0.1

# Redis - DISABLED (No Redis dependency)
# REDIS_CLIENT=phpredis
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# Mail Configuration
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@{DOMAIN}"
MAIL_FROM_NAME="${APP_NAME}"

# AWS (Skip for now)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# PUSHER CONFIGURATION - DISABLED for production
# PUSHER_APP_ID=1977071
# PUSHER_APP_KEY=b4fab1fabaec7b150a9e
# PUSHER_APP_SECRET=ebd2bcc4144fff5ceaca
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=ap1

# MIDTRANS CONFIGURATION
MIDTRANS_SERVER_KEY=SB-Mid-server-JDrEisrRJx70hjuMlssQosWD
MIDTRANS_CLIENT_KEY=SB-Mid-client-LG9Sij2hx9sn81ek
MIDTRANS_IS_PRODUCTION=false
MIDTRANS_IS_SANITIZED=true
MIDTRANS_IS_3DS=true

# Production URLs
PRODUCTION_URL=https://{DOMAIN}
WEBHOOK_URL=https://{DOMAIN}/api/webhook/midtrans
FRONTEND_URL=https://{DOMAIN}

# MQTT Configuration for ESP32 communication - ACTIVE
HIVEMQ_HOST=16d97e84c4364ffa9d0e5a0f0fa09165.s1.eu.hivemq.cloud
HIVEMQ_PORT=8883
HIVEMQ_USERNAME=hivemq.webclient.1745310839638
HIVEMQ_PASSWORD="UXNM#Agehw3B8!4;>6tz"
MQTT_CLIENT_ID=laravel_kost_production
MQTT_TOPIC_PREFIX=kost_system

# CORS CONFIGURATION - Domain Ready
SANCTUM_STATEFUL_DOMAINS={DOMAIN}
SESSION_DOMAIN={DOMAIN}

# Vite Configuration (For frontend)
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"